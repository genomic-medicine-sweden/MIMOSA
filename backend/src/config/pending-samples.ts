// Number of days before an unmatched pending sample is automatically removed.
// Increase this if samples are expected to take longer to arrive.
export const pendingSamplesConfig = {
  expiryDays: 30,
};
