# Set to false to skip Bonsai entirely and cluster only from chewBBACA-imported data.
# If Bonsai-sourced features already exist in the database, the pipeline will
# automatically include Bonsai anyway to avoid incomplete clustering.
AUTOMATION_BONSAI_ENABLED=true

# Internal URL for the Bonsai API (used inside Docker)
BONSAI_API_INTERNAL=http://host.docker.internal

# Service account credentials
AUTOMATION_BONSAI_USERNAME=automation_changeme
AUTOMATION_BONSAI_PASSWORD=automation_changeme
AUTOMATION_MIMOSA_USERNAME=automation_changeme
AUTOMATION_MIMOSA_PASSWORD=automation_changeme

# Profiles and groups (comma-separated; leave empty for all)
AUTOMATION_PROFILES=
AUTOMATION_GROUPS=

# Processing mode — at most one should be true
# UPDATE_ONLY: metadata sync only | RE_CLUSTER: force clustering
AUTOMATION_UPDATE_ONLY=false
AUTOMATION_RE_CLUSTER=false

# Run similarity analysis after clustering (default: false)
AUTOMATION_RUN_SIMILARITY=false

# Scheduler: interval in hours, and whether to run on startup
AUTOMATION_SCHEDULE_HOURS=4
AUTOMATION_RUN_ON_STARTUP=true

# Watch directory: path inside the container to the watch config JSON file.
# Leave commented out to disable. See watch-dirs.json for format.
#CHEWBBACA_WATCH_CONFIG=/watch-dirs.json
#CHEWBBACA_WATCH_INTERVAL=24
